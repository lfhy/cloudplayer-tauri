{
  "name": "cloudplayer-tauri",
  "private": true,
  "version": "2.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "android:build": "tauri android build -t aarch64",
    "android:build:apk": "tauri android build -t aarch64 --apk",
    "android:build:all-abis": "tauri android build",
    "android:build:arm64": "tauri android build -t aarch64 --apk",
    "android:dev": "tauri android dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.0",
    "@tauri-apps/plugin-os": "^2.3.0"
  }
}
